# values 에서 serviceAccount.create: true 로 만들면 이 파일은 필요 없다.
# 이미 존재하는 SA 에 annotation 만 붙여야 할 때 쓴다.
apiVersion: v1
kind: ServiceAccount
metadata:
  name: loki
  namespace: monitoring
  annotations:
    eks.amazonaws.com/role-arn: arn:aws:iam::000000000000:role/skills-loki-role
