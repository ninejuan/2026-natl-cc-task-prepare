# keda-operator ServiceAccount 가 이 Role 을 assume 할 수 있어야 한다 (IRSA 또는 Pod Identity).
apiVersion: keda.sh/v1alpha1
kind: TriggerAuthentication
metadata:
  name: sqs-auth
  namespace: app
spec:
  podIdentity:
    provider: aws
    roleArn: arn:aws:iam::000000000000:role/skills-keda-sqs-role
