# ServiceMonitor 의 endpoints[].port 는 이 Service 의 포트 name 을 가리킨다. 번호가 아니다.
apiVersion: v1
kind: Service
metadata:
  name: app-metrics
  namespace: app
  labels:
    app: app
spec:
  selector:
    app: app
  ports:
    - name: metrics
      port: 9090
      targetPort: 9090
