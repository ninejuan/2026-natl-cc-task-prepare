# IP 대역 묶음에 이름을 붙여 정책에서 selector 로 참조한다.
# "특정 대역을 차단하라" 요구를 정책마다 CIDR 을 늘어놓지 않고 처리한다.
apiVersion: projectcalico.org/v3
kind: GlobalNetworkSet
metadata:
  name: blocked-ranges
  labels:
    role: blocked
spec:
  nets:
    - 198.51.100.0/24
    - 203.0.113.0/24
